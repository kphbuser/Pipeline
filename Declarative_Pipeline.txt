pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				sh 'java -version'
				sh 'echo "Welcome to declarative_Pipeline"'
				
                		sh '''
                    		echo "Multiline shell steps"
                 		ls –lah
                		'''
			}
		}
	}
}